'use client';
import {Shell} from '@/components/ui'; import {useApp} from '@/components/app-provider'; import {useRouter} from 'next/navigation'; import {selectQuestions} from '@/lib/questions'; import {newSession} from '@/lib/storage'; import {Zap,Sun,Layers,ArrowRight} from 'lucide-react';
export default function Select(){const{data,updateSession}=useApp();const router=useRouter();function start(type:'quick'|'weekly'|'deep'){const id=crypto.randomUUID();const s=newSession(type,data.settings.partnerNames,selectQuestions(type,id).map(q=>q.id));updateSession(s);router.push(`/check-in/${s.id}/partner-a`)}const cards=[['quick','Quick check-in','About 5 minutes','A brief temperature check for right now.',Zap],['weekly','Weekly relationship check-in','About 15 minutes','Notice the week, name needs, and choose a next step.',Sun],['deep','Deeper conversation','About 25 minutes','Slow down and explore a recurring pattern with curiosity.',Layers]] as const;return <Shell back="/setup"><section className="page-intro"><span className="step-label">CHOOSE YOUR PACE</span><h1>What kind of pause<br/>do you need today?</h1><p>There’s no wrong choice. Pick what fits the time and energy you both have.</p></section><div className="type-grid">{cards.map(([id,title,time,desc,Icon])=><button key={id} onClick={()=>start(id)}><span className="type-icon"><Icon/></span><span><small>{time}</small><strong>{title}</strong><p>{desc}</p></span><ArrowRight/></button>)}</div><p className="center-note">You can leave and return at any time. Your progress is saved locally.</p></Shell>}
